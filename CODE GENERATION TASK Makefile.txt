=== CODE GENERATION TASK ===
File: Makefile

Purpose:
Build the project, compile `video_metric`, and clean build artifacts.

Context Summary:
`Makefile` orchestrates the compilation of the C sources, links the executable, and provides cleanup targets. It must invoke `gcc` with GNU‑C11-compatible flags and ensure that the local `ffmpeg` binary is present.

Requirements:
- Compile all `.c` files in `src/` into object files.
- Link the objects into a single executable named `video_metric`.
- Ensure the executable and `ffmpeg` are placed in the project root.
- Provide a `clean` target that removes the executable, temporary files, and any generated logs.
- Verify that `gcc` and `ffmpeg` are available before building; if not, emit an error.

Functions to Implement:
- `int all(void)` – compile and link.
- `int clean(void)` – remove build artifacts.

Input Data:
- Source files list (implicit from `src/` directory).
- Paths to `gcc`, `ffmpeg`, and the output directory.

Output Data:
- `video_metric` executable in the project root.
- `ffmpeg` binary remains untouched.
- No output files besides the executable and clean logs.

Pseudocode:
```
define SRC_DIR = src
define OBJ_DIR = obj
define BIN = video_metric

target all:
    create OBJ_DIR if not exist
    for each .c file in SRC_DIR:
        compile to corresponding .o in OBJ_DIR with flags
    link all .o files into BIN
    copy ffmpeg to BIN directory if necessary

target clean:
    delete OBJ_DIR
    delete BIN
    delete any vmaf.json logs

